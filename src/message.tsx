// PascalCase is used for file names and camelCase is used for the component name.
function Message() {
    //Jsx : Java Spript XML
    const name = 'Jan';
    if (name)
        return <h1>Hello {name}</h1>;
    return <h1>Hello World</h1>;

}

export default Message;
